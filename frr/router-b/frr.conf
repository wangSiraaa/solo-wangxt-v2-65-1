hostname router-b
log file /var/log/frr/frr.log
!
! Nothing policy-specific is pre-installed; the backend pushes an exact
! copy of a snapshot prefix-list for the duration of one validation run
! and removes it afterwards.
!
line vty
